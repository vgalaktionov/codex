import { Heading, VStack } from '@chakra-ui/react';

const DiceRollHistory = () => {
    return (
        <VStack>
            <Heading>Dice Roll History</Heading>
        </VStack>
    );
};

export default DiceRollHistory;
