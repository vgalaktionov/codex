import { Heading, VStack } from '@chakra-ui/react';

const Sheet = () => {
    return (
        <VStack>
            <Heading>Character Sheet</Heading>
        </VStack>
    );
};

export default Sheet;
